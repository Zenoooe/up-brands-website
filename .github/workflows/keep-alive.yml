name: Keep Supabase Alive

on:
  schedule:
    # Runs at 00:00 UTC every day
    - cron: '0 0 * * *'
  workflow_dispatch: # Allows manual run

jobs:
  ping-supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase URL
        run: |
          # Replace with your actual project URL or a specific API endpoint
          curl -I https://traenewweboa54.vercel.app/api/keep-alive || echo "Ping failed, but workflow continues"
