# SEO & IndexNow Finalization Plan

Based on the provided IndexNow documentation and your new key `8a942aefb20f42c8b4bae673d0fc3df3`, here is the plan to fix the duplicate SEO tags and correctly implement IndexNow.

## 1. Fix "Duplicate Tags" (Crucial for SEO)
We must remove the static tags from `index.html` because `SEO.tsx` (React Helmet) is already injecting them. Having both causes the "More than one tag" error.

*   **Edit `index.html`**:
    *   Remove `<title>...</title>`
    *   Remove `<meta name="description" ... />`
    *   Remove `<h1 style="...">...</h1>` (The hidden H1)
    *   **Keep**: `<meta charset>`, `<meta viewport>`, JSON-LD Schema, and verification tags.

## 2. Implement New IndexNow Key
We will switch from the old key to the new one you generated: `8a942aefb20f42c8b4bae673d0fc3df3`.

*   **Delete Old Key File**: Remove `public/a3cd48afa2594bc6b12faaa904168506.txt`.
*   **Create New Key File**: Create `public/8a942aefb20f42c8b4bae673d0fc3df3.txt` containing only the key string `8a942aefb20f42c8b4bae673d0fc3df3`.
*   **Update API (`api/indexnow.ts`)**: Change the `key` variable to `8a942aefb20f42c8b4bae673d0fc3df3`.
*   **Update Script (`scripts/submit-sitemap-indexnow.js`)**: Update the `KEY` constant.

## 3. Verify & Submit
1.  **Deploy**: Push changes to production.
2.  **Verify Key**: Ensure `https://www.up-brands.com/8a942aefb20f42c8b4bae673d0fc3df3.txt` is accessible.
3.  **Run Submission**: Execute `npm run sitemap:submit` to force a new submission to Bing/IndexNow.
